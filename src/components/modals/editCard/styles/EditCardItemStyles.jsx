import {StyleSheet} from "react-native"

export const styles = StyleSheet.create({
    saveButton: {
        marginTop: 50,
        shadowColor: "#8ddc5c",
        shadowOffset: {
            width: 0,
            height: 8,
        },
        shadowOpacity: 0.5,
        shadowRadius: 10,
    }
})